startCommand:
  type: http
  command: uvicorn
  args: ["app:app", "--host", "0.0.0.0", "--port", "8000"]
  port: 8000           # Dockerfile 과 동일

# 런타임 옵션이 아예 필요 없다면 아래 블록을 제거해도 무방합니다.
configSchema:
  type: object
  description: "Logispot MCP · 별도 설정 불필요"
  exampleConfig: {}


