startCommand:
  type: http
  command: python
  args: ["server.py"]
  port: 8000

configSchema:               # ← 없어도 동작하지만 UI 경고 제거용
  type: object
  description: "Demo – no runtime config required"
  exampleConfig: {}
