startCommand:
  type: remote
  url: https://fastapi-mcp-sse.onrender.com/sse   # Render 배포 URL
  transport: sse                                  # 클라이언트‑→서버 연결 방식
configSchema:
  type: object
  description: "Public demo – no config required"
  exampleConfig: {}

