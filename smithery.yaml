version: 1

startCommand:
  type: http

configSchema:
  type: object
  required:
    - endpoint
  properties:
    endpoint:
      type: string
      title: "MCP Endpoint"
      description: "Custom endpoint to route to"
      default: "/sse"

exampleConfig:
  endpoint: "/sse"

build:
  dockerfile: Dockerfile
