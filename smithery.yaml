version: 1

startCommand:
  type: http
  configSchema:
    type: object
    required:
      - endpoint
    properties:
      endpoint:
        type: string
        default: "/sse"
  exampleConfig:
    endpoint: "/sse"

build:
  dockerfile: Dockerfile