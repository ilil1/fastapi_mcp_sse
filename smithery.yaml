startCommand:
  type: http
  command: uvicorn
  args: ["src:app", "--host", "0.0.0.0", "--port", "8000"]
  port: 8000            # <- Smithery가 컨테이너 내부에서 연결할 포트

configSchema:           # (없으면 UI에 회색 박스 경고)
  type: object
  description: "Demo – runtime config 없음"
  exampleConfig: {}


