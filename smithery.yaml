startCommand:
  type: http
  command: uvicorn
  args: ["app:app", "--host", "0.0.0.0", "--port", "8000"]
  port: 8000

configSchema:        # ← 넣어두면 “Configuration Unavailable” 메시지도 사라집니다
  type: object
  description: "Demo – runtime config 없음"
  exampleConfig: {}

